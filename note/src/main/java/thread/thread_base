Java多线程基础

    1、多线程程序在较低层次上扩展了多任务的概念，一个程序可以同时执行多个任务。通常，每一个任务成为一个线程（Thread）,它是线程控制的简称。
       可以同时运行一个以上的线程的程序成为多线程程序。

    2、线程与进程的区别：
        1）、一个进程可以拥有多有线程
        2）、每个进程拥有自己的一整套变量，而线程则共享数据
        3）、在某些操作系统中，，与进程相比较，线程更轻量级，创建，撤销一个线程，比启动新进程的开销要小的多

     